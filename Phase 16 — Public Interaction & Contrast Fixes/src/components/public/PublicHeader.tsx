import { useEffect, useMemo, useState } from 'react'
import { Link, useLocation } from 'react-router-dom'
import { List, MagnifyingGlass, X } from '@phosphor-icons/react'
import BrandLogo from './BrandLogo'
import IconButton from '@/components/ui/IconButton'
import BagIcon from '@/components/ui/BagIcon'
import { getPublicProducts } from '@/data/catalog'
import type { Product } from '@/types/product'
import { useRequestDraft } from '@/state/RequestDraftContext'
function navClass(active:boolean){return active?'relative text-sm font-bold text-[var(--color-text)]':'relative text-sm font-medium text-[var(--color-text-muted)] transition-colors hover:text-[var(--color-text)]'}
function ActiveUnderline({active}:{active:boolean}){return active?<span aria-hidden="true" className="absolute -bottom-2 left-1/2 h-0.5 w-5 -translate-x-1/2 rounded-full bg-[var(--color-accent)]"/>:null}
export default function PublicHeader(){const {itemCount,openRequest}=useRequestDraft();const location=useLocation();const [searchOpen,setSearchOpen]=useState(false);const [menuOpen,setMenuOpen]=useState(false);const [query,setQuery]=useState('');const [products,setProducts]=useState<Product[]>([]);useEffect(()=>{if(searchOpen&&products.length===0)void getPublicProducts().then(setProducts).catch(()=>undefined)},[searchOpen,products.length]);const results=useMemo(()=>{const value=query.trim().toLowerCase();return value?products.filter(product=>(product.name+' '+(product.shortDescription||'')).toLowerCase().includes(value)).slice(0,6):[]},[products,query]);const nav=<><Link to="/" className={navClass(location.pathname==='/')}>الرئيسية<ActiveUnderline active={location.pathname==='/'}/></Link><Link to="/products" className={navClass(location.pathname==='/products'||location.pathname.startsWith('/product'))}>قائمتنا<ActiveUnderline active={location.pathname==='/products'||location.pathname.startsWith('/product')}/></Link><Link to="/business" className={navClass(location.pathname.startsWith('/business'))}>للشركات<ActiveUnderline active={location.pathname.startsWith('/business')}/></Link></>;const searchBox=<div className="relative w-full max-w-xl"><input autoFocus value={query} onChange={event=>setQuery(event.target.value)} placeholder="ابحث عن منتج..." className="h-11 w-full rounded-xl border border-[var(--color-border)] bg-white px-4 text-right outline-none transition-shadow focus:border-[var(--color-accent)] focus:ring-4 focus:ring-[var(--color-accent)]/10"/>{results.length?<div className="absolute top-[calc(100%+8px)] z-50 w-full overflow-hidden rounded-xl border border-[var(--color-border)] bg-white shadow-xl">{results.map(product=><Link key={product.id} to={'/product/'+product.slug} onClick={()=>{setSearchOpen(false);setQuery('')}} className="block border-b border-[var(--color-border)] px-4 py-3 text-right text-sm hover:bg-[var(--color-bg)]">{product.name}</Link>)}</div>:null}</div>;return <header className="sticky top-0 z-40 border-b border-[var(--color-border)] bg-[color:var(--color-surface)]/95 backdrop-blur"><div className="mx-auto flex h-16 max-w-[1440px] items-center justify-between gap-3 px-4 md:h-20 md:px-16"><BrandLogo compact className="md:hidden"/><BrandLogo className="hidden md:inline-flex"/>{searchOpen?<div className="hidden flex-1 animate-[fadeIn_.2s_ease-out] justify-center px-4 md:flex">{searchBox}</div>:<nav className="hidden items-center gap-8 md:flex" aria-label="التنقل الرئيسي">{nav}</nav>}<div className="flex items-center gap-1.5"><IconButton label={searchOpen?'إغلاق البحث':'البحث في القائمة'} onClick={()=>{setSearchOpen(value=>!value);setQuery('')}}>{searchOpen?<X size={20}/>:<MagnifyingGlass size={20}/>}</IconButton><IconButton label="الطلب" onClick={openRequest}><span className="relative inline-flex"><BagIcon/>{itemCount>0?<span className="absolute -left-2 -top-2 min-w-5 animate-bounce rounded-full bg-[var(--color-accent)] px-1 text-center text-[10px] font-bold leading-5 text-white">{itemCount}</span>:null}</span></IconButton><button type="button" onClick={()=>setMenuOpen(value=>!value)} className="grid size-10 place-items-center rounded-lg border border-[var(--color-border)] md:hidden" aria-label="القائمة">{menuOpen?<X size={20}/>:<List size={20}/>}</button></div></div>{searchOpen?<div className="border-t border-[var(--color-border)] p-3 md:hidden">{searchBox}</div>:null}{menuOpen?<nav className="flex items-center justify-center gap-7 border-t border-[var(--color-border)] px-4 py-4 md:hidden" aria-label="التنقل الرئيسي للجوال">{nav}</nav>:null}</header>}

import ImageUploadField from '@/components/dashboard/ImageUploadField'
type Props={values:string[];onChange:(values:string[])=>void;storagePath:string;disabled?:boolean}
export default function HeroImageSlotsField({values,onChange,storagePath,disabled=false}:Props){const slots=[0,1,2].map(index=>values[index]??'');const setSlot=(index:number,value:string)=>{const next=[...slots];next[index]=value;onChange(next)};return <section className="space-y-4"><div className="text-right"><strong>صور الـHero</strong><p className="mt-1 text-xs text-[var(--color-text-muted)]">ارفع حتى ثلاث صور من نفس المكان، ثم انتقل لبقية محتوى الصفحة.</p></div><div className="grid gap-4 xl:grid-cols-3">{slots.map((value,index)=><div key={index} className="min-w-0 rounded-2xl border border-[var(--color-border)] bg-[var(--color-bg)] p-3"><ImageUploadField label={'Hero '+(index+1)} value={value} onChange={nextValue=>setSlot(index,nextValue)} storagePath={storagePath+'/'+(index+1)} help={index===0?'الصورة الأساسية.':'صورة اختيارية للتبديل.'} recommendedSize="1400 × 1200 px · 7:6" disabled={disabled}/></div>)}</div></section>}